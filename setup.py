from setuptools import setup, find_packages

setup(
    name="syllabifier",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    description="A Python port of the Nepali Syllabifier",
    author="Firoj",
)